version: "3.8"

# docker-compose -f docker-compose-dev.yml up -d --build
services:
  api-dev:
    build:
      context: ./
      dockerfile: Dockerfile
    working_dir: /app
    container_name: ac2-api-dev
    ports:
      - 6000:8080
    restart: always
    